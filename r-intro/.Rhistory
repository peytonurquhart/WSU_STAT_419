# can you draw a circle
# can you loop through "n" points
# ... and connect "n-1" elements
# for(i in 1:n) ... for(j in 1:(n-1))
plot(n,h);
{
# can you draw a circle
# can you loop through "n" points
# ... and connect "n-1" elements
# for(i in 1:n) ... for(j in 1:(n-1))
plot(n,h);
}
install.packages("plotrix")
draw.circle
plotrix
help(plotrix)
??plotrix
clear
plot(draw.circle(x,y,radius,nv=100,border=NULL,col=NA,lty=1,density=NULL,angle=45,lwd=1));
install.packages(ggplot)
"ggplot2"
install.packages(ggplot2)
install.packages("ggplot2")
ggplot
ggplot2
print(p)
print(p)
source('C:/_git_/peyton.urquhart/functions/functions-intro.R')
{
p = getGgUnitCirclePlot(100)
# Add 'n' points evenly distributed on the unit circle
hs_dat = circleData(center=c(1,1), r=1, npoints = n)
p = p + geom_point(data=hs_dat)
# can you draw a circle
# can you loop through "n" points
# ... and connect "n-1" elements
# for(i in 1:n) ... for(j in 1:(n-1))
print(p)
}
Q
Q.
q
qdlsskmd
quit
clear
cls
c
wow
loadDataIMDB();  # will populate a global list of data:  imdb.data
library(humanVerseWSU);
library(humanVerseWSU);
# library(humanVerseWSU);
library(devtools);
detach(package:humanVerseWSU);
install.packages
